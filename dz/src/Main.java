import java.util.Arrays;
import java.util.Comparator;
import java.util.Random;

public class Main {
    public static void main(String[] args) {

        /* Stack and Queue

        StackAndQueue<Integer> queue = new StackAndQueue<>();

        // Тестируем добавление элементов
        queue.enqueue(1);
        queue.enqueue(2);
        queue.enqueue(3);

        System.out.println(queue.isEmpty()); // false

        System.out.println(queue.dequeue()); // 1
        System.out.println(queue.peek());    // 2
        System.out.println(queue.dequeue()); // 2
        System.out.println(queue.dequeue()); // 3

        // Проверка на пустую очередь
        System.out.println(queue.isEmpty()); // true

        */

        /* ComplexityOfAlgorithms

        int[] arr1 = {1, 3, 5, 7};
        int[] arr2 = {2, 4, 6, 8};

        int[] mergedArray = RecursionAlgorithms.merge(arr1, arr2);

        // Выводим объединенный массив
        System.out.print("Объединенный массив: ");
        for (int num : mergedArray) {
            System.out.print(num + " ");
        }


        Random random = new Random();
        int size = 20; // Размер массива
        int[] arr = new int[size];

        // Генерация случайного массива
        for (int i = 0; i < size; i++) {
            arr[i] = random.nextInt(100); // Заполняем числами от 0 до 99
        }

        System.out.println("Исходный массив: " + Arrays.toString(arr));
        ComplexityOfAlgorithms.quickSort(arr, 0, arr.length - 1); // Сортировка массива
        System.out.println("Отсортированный массив: " + Arrays.toString(arr));

        */
        
    }
}