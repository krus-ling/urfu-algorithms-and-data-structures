# Билеты:


------------------------------------------------------------------------------------------------------------------------


# 🎈 Алгоритм бинарного поиска


## 1. Основные термины (бинарный поиск, разделяй и властвуй)

`Бинарный поиск` (binary search, двоичный поиск, метод дихотомии) — это
алгоритм поиска элемента в **отсортированном** массиве, использующий дробление массива на половины.

#### Принцип работы:

* Мы задаем левую и правую границы (0 и длина массива - 1)
И находим середину

* Наш ключ сравнивается со средним элементом в массиве. Если они равны, то возвращаем элемент.

* В противном случае, поиск осуществляется аналогично в левой или правой частях массива.
При этом левая и правая границы смещаются в соответствующую сторону


Сложность алгоритма `log2(n)`


`"Разделяй и властвуй"` — это подход к решению задач, 
при котором исходная задача последовательно делиться до тех пор, пока не получится просто решение.


## 2. Реализация бинарного поиска

    1. 
    2. 


------------------------------------------------------------------------------------------------------------------------


# 🎆 Хеш-таблицы и хеш-функции


## 3. Основные термины (хеш-таблица, хеш-функция, коллизия и пр.)

`Хеш-таблица` (HashMap) — это структура данных, позволяющая хранить соответствие между ключом и значением.

#### Особенности:

* Ключи не обязаны идти друг за другом


* Можно удалять и вставлять элементы без сдвигов


* В качестве ключа можно использовать числа, строки и даже объекты


* Для хранения значений используется массив

`Хеш-функция` — это функция для работы с хеш-таблицами.
Она может по ключу вычислить индекс в массиве и сохранить соответствующее значение.

`Динамические структуры данных` — это структуры данных, память под которые выделяется по мере необходимости.
В памяти может изменяться не только число элементов, но и характер связи между ними.
Можно добавлять и удалять элементы без сдвига.

`Динамический массив` — это массив, размер которого может изменяться во время исполнения программы.

`Хеш` — это значение хеш-функции, которое однозначно зависит от входных данных.

`Коллизия` — это ситуация, когда хеш-функция возвращает одинаковое значение для разных ключей.

* Важно, чтобы время выполнения хеш-функции не зависело от размера массива значений.


## 4. Способы разрешения коллизий

* `Открытая адремация` — при возникновении коллизий просто попробовать положить значение
в какой-то другой индекс массива значений (`Линейное пробирование` — пробирование,
при котором последовательно просматриваются элементы массива значений в поисках места вставки)


* `Метод цепочек` — все элементы с одинаковым хешем хранятся в связанном списке, привязанном к соответствующему
индексу массива. Таким образом каждая ячейка массива содержит указатель на начало цепочки элементов.


* `Рехеширование` — при достижении определенного коэффициента заполнения хеш-таблица пересоздается с увеличенным
размером массива, и все элементы перераспределяются по новой хеш-функции. 


* `Двойное хеширование` — используется в открытой адресации. При коллизии применяется вторая хеш-функция для определения
следующей свободной ячейки.


* Использование `вспомогательных структур`, например, деревья в HashMap


* `Квадратичное пробирование` — один из видов открытой адресации. При коллизии следующая ячейка ищется не линейно, а по
формуле с квадратичным смещением, что уменьшает вероятность скопления кластеров по сравнению с линейным пробированием.


------------------------------------------------------------------------------------------------------------------------


# 🧨 Связные списки

## 5. Типы сложностей алгоритма

* Константная `O(1)` — время работы алгоритма не зависит от входных данных 


* Линеарифметическая `O(n log(n))` — линейно-логарифмическая зависимость от входных данных


* Линейная `O(n)`


* Логарифмическая `O(log(n))`


* Квадратичная `O(n^2)`


## 6. Основные термины (односвязный список, стек, трассировка и пр.)

`Односвязный список` — это динамическая структура данных, состоящая из элементов, каждый из которых содержит значение
 и хранит ссылку на следующий элемент.

* Для того, чтобы работать со списком, достаточно хранить ссылку на самый первый элемент.


* Односвязный список позволяет вставлять элементы в середину или начало массива быстрее, чем за линейное время. 

`Рекурсивная функция` — это функция, которая может вызывать саму себя.

`Стек` — это линейная структура данных, для которой определенны 2 основные операции:
добавление элемента в конец и удаление элемента из конца.

* Работает по принципу LIFO (last in – first out).

`Трассировка` — это отчет, содержащий вызовы функций, сделанных в коде в определенный момент.

* Также называется как: **stack trace**, **stack traceback** и **backtrace**.


## 7. Реализации записи элементов в список

    1. 
    2. 


------------------------------------------------------------------------------------------------------------------------


# ✨ Стек и очередь

## 8. Основные характеристики двусвязного списка, очереди и деки

`Двусвязный список` — это динамическая структура данных, состоящая из элементов,
каждый из которых содержит значение, ссылку на следующий и предыдущий элемент.

* Чтобы работать со списком, достаточно хранить ссылку на самый первый и последний элементы.

**Характеристики:**

* Объединение двух списков `O(1)`


* Вставка элемента в начало/конец/середину `O(1)`


* Удаление элемента из начала/конца/середины `O(1)`


* Поиск элемента `O(1)`


* Обращение к элементу по индексу `O(n)`


`Очередь` — это линейная структура данных, для которой определенны 2 основных операции:
добавление элемента в конец и удаление элемента из начала

* Работает по принципу FIFO (first in – first out).


* `Персистентная очередь` — очередь, хранящая все состояния, которые у нее были, и позволяющая получить к ним доступ.
Реализуется при помощи пяти или шести стеков.

**Характеристики:**

* Можно реализовать при помощи других физических структур, например,
при помощи двусвязного списка или зацикленного массива.


* Вставка и удаление элемента `O(1)`

`Дек` — линейная структура данных, для которой определенны 2 основные операции:
добавление элемента в конец или начало и удаление элемента из конца или начала.

**Характеристика:**

* Можно реализовать при помощи других структур данных, например, с помощью динамического зацикленного массива


* Вставка и удаление элемента `O(1)`


## 9. Реализация очереди с помощью двух стеков.

    1.
    2. 


------------------------------------------------------------------------------------------------------------------------


# 🎉 Основы алгоритмов сортировки

## 10. Основные термины (сортировка, компаратор, лексикографический порядок)

`Сортировка` — это упорядочивание набора однотипных данных по возрастанию или убыванию.

`Лексикографический порядок` — это отношение линейного порядка на множестве слов.
Порядок по аналогии с сортировкой по алфавиту в словаре.

`Компаратор` — это функция, созданная для сравнения двух объектов.


## 11. Определения и характеристики сортировок (пузырьковая, вставками и пр.)

`Пузырьковая сортировка` — это алгоритм сортировки, в котором элементы в массиве сравниваются попарно.
Если два соседних элемента стоят в неправильном порядке – они меняются местами.
Проходы по массиву повторяются, пока на очередном проходе не окажется, что обмены больше не нужны.


На каждом полном проходе самый большой элемент «всплывает» в конец массива, как пузырёк — отсюда и название.

**Характеристики:**

* Работает за `O(n^2)`


* В лучшем случае за `O(n)`


* Количество обменов и сравнений `O(n^2)`


`Сортировка выбором` — это алгоритм сортировки, в котором на каждой итерации ищется наименьший элемент среди
неотсортированных, после чего он меняется местами с первым неотсортированным элементом. И так до конца массива.

**Характеристики:**

* Работает за `O(n^2)`


* Количество обменов `O(n)`


`Сортировка вставками` — это алгоритм сортировки, в котором каждый новый элемент берётся и вставляется в
отсортированную часть массива в нужное место. Сортировка происходит как будто «вручную» — по одному элементу.

**Характеристики:**

* Работает за `O(n^2)`


* В лучшем случае за `O(n)`


* Количество обменов `O(n^2)`


* Является устойчивой (`Устойчивая сортировка` — это сортировка, не меняющая порядка объектов, которые считаются
равными при сравнении)


`Сортировка подсчетом` — это алгоритм сортировки, в котором создаётся вспомогательный массив, где подсчитывается
количество каждого элемента, после чего результат собирается по количеству в отсортированном порядке.

**Характеристики:**

* Нельзя реализовать быстрее чем за `O(n log(n))`


* Данная сортировка не целесообразна, если размер входного массива сильно меньше,
чем значение его максимального элемента


## 12. Реализация сортировок

    1. 


------------------------------------------------------------------------------------------------------------------------


# 🎊 Рекурсивные алгоритмы

## 13. Определения рекурсивного перебора и битовой маски

`Рекурсивный перебор` — это технология, которая используется для создания более универсальных функций и уменьшения
размера кода.

`Битовая маска` — это хранение 0 или 1 в каждом бите. Используется для экономии памяти.


## 14. Определение и характеристики сортировки слияния

`Сортировка слиянием` — это алгоритм сортировки, рекурсивно сортирующий левую и правую половину массива и после этого
объединяющий отсортированные половины с помощью функции слияния.

**Характеристики:**

* Глубина рекурсии `log(n)`


* Асимптотическая сложность `O(n log(n))`


* Недостаток – большие затраты на дополнительную память


## 15. Реализация двух массивов с помощью рекурсии

    1. 


## 16.  Реализация сортировки слиянием

    1. 


------------------------------------------------------------------------------------------------------------------------


# 🎃 Сложность алгоритмов

## 17. Основные термины (быстрая сортировка, медиана и пр.)

`Быстрая сортировка` (сортировка Хоара) — это алгоритм сортировки, имеющий следующие основные шаги:

1. Найти разделяющий элемент

2. Перераспределить остальные элементы так, чтобы все элементы, которые меньше разделяющего оказались слева о него, а
все элементы, которые больше или равны – справа от него. 

3. Затем в левой и правой частях рекурсивно повторяем то же самое

Рекурсия не применяется если в массиве 1 элемент или ни одного.

`Медиана` — это такое число в наборе данных, которое находится ровно посередине, когда все числа отсортированы по
возрастанию. То есть половина чисел будет меньше медианы, в другая – больше.

`Математическое ожидание` — это понятие теории вероятностей, означающее среднее взвешенное значение случайной величины.
Для дискретных чисел взвешивание осуществляется по вероятностям возможных значений.


## 18. Характеристики быстрой сортировки

* Быстрая сортировка устраняет недостаток сортировки слиянием, заключающийся в использовании дополнительной памяти


* В среднем быстрая сортировка работает за `O(n log(n))`.
Это значит, что на каждом уровне деления массивы мы перебираем все элементы за `O(n)`,
а всего таких уровней `log(n)`


* В худшем случае может работать за `O(n^2)` из-зи уровней разбиения.
Математическое ожидание глубины уровней разбиения `O(log(n))`, поэтому на практике алгоритм быстрой сортировки
практически всегда будет работать за `O(n log(n))`


## 19. Реализация быстрой сортировки

    1. 


------------------------------------------------------------------------------------------------------------------------


# 🎄 Жадные алгоритмы

## 20. Основные термины (жадный алгоритм, комбинаторные задачи и пр.)

`Жадный алгоритм` — это такой алгоритм, при котором на каждом шаге выбирается самое оптимальное решение на данный момент,
допуская, что конечное решение также окажется оптимальным

`Комбинаторные задачи` — это выбор из заданного множества, обладающего теми или иными свойствами

`Стресс-тестирование` — это один из видов тестирования программ, заключающийся в сравнении результатов простого,
не оптимального, но гарантировано правильного решения с результатом "быстрого" решения, в котором есть сомнения

`Алгоритм Хаффмана` — это жадный алгоритм оптимального префиксного кодирования алфавита. Используется для сжатия текста.

**Состоит из 2 основных этапов:**

1. Построение оптимального кодового дерева (чем чаще встречается символ, тем ближе он к вершине)

2. Построение отображения "код-символ" на основе построенного кодового дерева


`Префиксный код в теории кодирования` — это способ кодирования, при котором ни одно закодированное слово не является
началом другого слова. Например, если в код входит слово а, то для любой непустой строки b слова ab не существует 


## 21. Реализация задачи про коробки

    1. 


------------------------------------------------------------------------------------------------------------------------


# 🎋 Деревья. Двоичные деревья поиска

## 22. Основные термины (вершина дерева, двоичное дерево поиска и пр.)

`Вершина дерева` — это элемент дерева, в котором хранятся данные. Каждая вершина соответствует определенному ключу

`Ребро дерева` — это элемент дерева – ссылка, соединяющая 2 вершины дерева

`Корень дерева` — это стартовая верхняя вершина дерева

`"Родитель" и "сын"` — это вершины, связанные отношением: из родителя есть ребро в сына

`Двоичное дерево поиска` — это двоичное дерево, в котором для любой вершины верно следующее:

1. Все элементы в левом поддереве <= значению в этой вершине

2. Все элементы в правом поддереве >= значению в этой вершине


`Сбалансированное дерево` — это дерево, в котором каждое поддерево сбалансировано и высота двух поддеревьев не отличается
более чем на единицу


## 23. Характеристики двоичного дерева

* Время работы не зависит от случайностей


* Памяти нужно ровно столько, сколько элементов в дереве


* Асимптотика времени работы не амортизированная


* Операции: вставка/удаление по ключу, обращение к элементу, обход элементов по возрастанию ключей,
поиск следующего за ключом элемента, поиск минимума/максимума


* Двоичное дерево — сбалансированное дерево. Его глубина всегда равна `O(log(n))`


## 24. Реализация поиска места и добавления элемента в двоичное дерево

    1. 


------------------------------------------------------------------------------------------------------------------------


# 🎍 Деревья. Обход в ширину и глубину

## 25. Основные термины (дерево, связный граф и пр.)

`Неориентированный граф G` — это структура, состоящая из вершин и ребер. Обозначается как **G = (V, E)**, где
**V** – это множество всех вершин, а **E** – множество ребер.
В неориентированном графе порядок вершин в ребре не важен

`Дерево` — это связный граф без циклов

`Связный граф` — это граф, у которого все вершины связаны между собой

`Цикл` — это путь ненулевой длины в графе, который начинается и заканчивается в одной и той же вершине

`Корневое дерево` — это дерево, в котором одна из вершин – корень

`Глубина вершины дерева` — это расстояние от корня до этой вершины, измеренное в количестве ребер


## 26. Характеристики графов и деревьев

* Граф часто используется на практике для представления связанных элементов. Например, городов, серверов или друзей
в социальной сети


* Деревья применяются для реализации быстрого доступа к элементу структуры, для представления иерархических структур


* Хранить деревья можно с помощью списка со ссылками на соседей, но это неудобно, например, при необходимости записи
в файл 


* Хранить деревья можно с помощью списка ребер или предков (для корневого дерева). На практике такое хранение
обеспечивает более быструю работу


## 27. Определения и характеристики обходов в глубину и ширину

`Обход дерева в глубину` — это алгоритм обхода дерева, при котором мы сначала идем как можно глубже в одну ветвь, прежде
чем перейти к другим

* Алгоритм начинает с корневой вершины


* Затем рекурсивно переходит к следующей, которая еще не посещена


* Когда идти дальше некуда, возвращается назад и исследует другие пути


Время работы `O(n + m)`, где **n** – количество вершин, а m – количество ребер. Дополнительная память `O(n)`

Обход в глубину часто используется для того, чтобы посчитать какую нибудь информацию для поддерева, например,
количество вершин


`Обход дерева в ширину` — это алгоритм обхода дерева, при котором мы обходим вершины по уровням

* Начинаем с корня – начального уровня


* Затем посещаем все вершины первого уровня (по сути дети корня)


* И так далее спускаемся ниже, пока не посетим все вершины самого глубокого уровня


Этот алгоритм не рекурсивный, значит не требует дополнительной памяти на рекурсию

Можно реализовать с помощью **двух массивов** (один – для хранения всех вершин на текущем уровне, второй – для хранения
всех вершин на следующем уровне) или с помощью **одной очереди** (на каждом шаге добавлять в очередь всех детей
посещаемой вершины и доставать следующую вершину из очереди)

Время работы `O(n + m)`
`O(n)` – дополнительная память на хранение очереди и меток посещенных вершин


## 28. Реализация обхода в глубину

    1. 


## 29. Реализация обхода в ширину с использованием двух массивов

    1.


------------------------------------------------------------------------------------------------------------------------


# 💩 Куча (Heap)

## 30. Основные термины (куча, очередь с приоритетами)

`Куча` — это структура данных типа бинарное дерево, которая поддерживает три операции:

1. Добавить элемент `O(log(n))`

2. Получить значение мин/макс элемента `O(1)`

3. Удалить мин/макс элемент `O(log(n))`

Работает куча быстрее, чем двоичное дерево поиска, несмотря на одинаковую асимптотику. При работе с кучей нет переходов
по ссылкам, скачков по памяти меньше, операции проще

`Очередь с приоритетами` — это структура данных, которая подразумевает добавление элемента в очередь с каким-либо
приоритетом. Элементы извлекаются не в порядке поступления, а в порядке приоритета, т.е. первым вернется самый
приоритетный элемент

Самый простой способ реализации очереди с приоритетами — при помощи кучи


## 31. Характеристики кучи

* В корне структуры хранится самый маленький элемент. Значение каждой родительской вершины меньше, чем все значения
в ее поддереве


* Для массива можно построить кучу несколькими способами


* Для быстрой работы операции нужно потребовать, чтобы куча была сильно сбалансирована. При этом все слои, кроме
последнего должны быть заполнены, а последний заполняется слева направо


* Порядок нумерации вершин соответствует порядку обхода в ширину по данной куче


* Благодаря имеющимся правилам организации структуры кучу можно реализовать в виде массива. Это обеспечит более
быструю работу операций


## 32. Реализация добавления элемента в кучу

    1. 


## 33. Реализация удаления элемента из кучи

    1. 


------------------------------------------------------------------------------------------------------------------------


# 🎏 Динамическое программирование

## 34. Основные термины (динамическое программирование, мемоизация,табуляция)


















































